env36_dj_by_example_common

python==3.6.8
django==2.2
django-taggit  # only latest ,without version specified

#install Postgresql
#database name: common
psycopg2

Pillow==5.1.0


redis is installed on VM virtual box, Ubuntu64 Clone, habrauser

habrauser$ src/redis-server --protected-mode no

django-braces==1.13.0